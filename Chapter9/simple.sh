#!/bin/bash

# This is a simple example of to write a delegate in shell script
# It assumes the first arguement is the input file, and the second
# argument is the output file. It just renames the input to the output name

cp $1 $2